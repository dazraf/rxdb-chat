/** Maximum size of a single uploaded file (bytes). */
export const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5 MB

/** Maximum total size of the offline upload queue (bytes). */
export const MAX_QUEUE_BYTES = 50 * 1024 * 1024; // 50 MB

/** Default sub that all users are subscribed to. */
export const DEFAULT_SUB_ID = 'general';

/** Number of posts per page for infinite scroll. */
export const POSTS_PAGE_SIZE = 100;
